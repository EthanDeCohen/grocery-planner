"""Local-first grocery price/deal planner.

Scrapes grocery deals, stores them in a local SQLite database, and lets you
compare prices/deals across stores from the CLI. Excel is an optional export
target, not the runtime.
"""

__version__ = "1.1.2"
