"""PySide6 desktop UI (GFP-14) — the first usable GUI slice of GFP-11.

Requires the optional ``gui`` extra (``pip install -e ".[gui]"``). Launch with
``python -m grocery_planner.gui`` or the ``gplan-gui`` console script.
"""
